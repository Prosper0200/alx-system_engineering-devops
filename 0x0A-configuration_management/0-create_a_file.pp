# This manifest creates a file in /tmp named school with the specified permissions,
# ownership, and contents.

file { '/tmp/school':
  ensure  => file,
  content => 'I love Puppet',
  mode    => '0744',
  owner   => 'www-data',
  group   => 'www-data',
}
